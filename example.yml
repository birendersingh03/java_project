apiVersion: apps/v1
kind: Deployment
metadata:
  name: example-deployment
  labels:
    role: app
spec:
  replicas: 2
  selector:
    matchLabels:
      role: app
  template:
    metadata:
      labels:
        role: app
    spec:
      containers:
      - name: app
        image: "13.233.184.68:8123/hello-world-2.1.0"
        resources:
          requests:
            cpu: 10m

